spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: false
        highlight_sql: false

fcm:
  firebase-config-path: "classpath:config/serviceAccountKey.json"